#pragma once

enum EJobTypes
{
    JOB_TYPE_NULL,
    JOB_TYPE_PARSE_TXT,
    JOB_TYPE_PARSE_JSON,
    JOB_TYPE_TXT_TO_JSON, 
    JOB_TYPE_MAX
};

enum EFileTypes
{
    FILE_TYPE_NULL,
    FILE_TYPE_MOBDROPINFO,
    FILE_TYPE_MAX
};
